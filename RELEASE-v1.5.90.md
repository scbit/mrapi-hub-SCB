# v1.5.90

- Corrige definitivamente `Nuevos / sin asignar` usando el mismo resolver completo del chat antes de mostrarlo.
- Resuelve aliases legacy del mismo cliente + línea, IDs CRM guardados, `hubConversationId` y fallback por teléfono.
- Si ya existe contacto o trato CRM, la conversación no aparece nunca en Nuevos.
- No modifica No leídos, historial, HUMAN/BOT, Recovery ni Gateway.
