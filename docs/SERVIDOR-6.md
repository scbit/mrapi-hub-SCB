# servidor-6

## Ajuste

- Sin cambios funcionales sobre `servidor-5`.
- Actualiza las etiquetas visibles, `health` y cache busting a `servidor-6` después de validar que el composer todavía mostraba `servidor-4`.
