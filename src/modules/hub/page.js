"use strict";
const express=require("express");const {getTenant}=require("../../core/tenant");const router=express.Router();
router.get("/",(req,res)=>{const t=getTenant();res.type("html").send(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>${t.product} · ${t.branding.shortName}</title><link rel="stylesheet" href="/assets/app.css"></head><body><div class="top"><b>${t.product}</b><span>${t.name}</span></div><main class="wrap"><span class="tag">v0.1 · tenant ${t.id}</span><h1>CRM + HUB + BANDEJA</h1><p class="muted">Base modular de MR API HUB ejecutándose correctamente.</p><div class="cards"><div class="card"><h2>Bandeja</h2><p>Listado paginado y búsquedas dirigidas, sin escaneos de 15.000 conversaciones.</p><code>/api/inbox/conversations</code></div><div class="card"><h2>CRM</h2><p>Contactos y tratos desacoplados del servidor principal.</p><code>/api/crm/*</code></div><div class="card"><h2>HUB</h2><p>Tenant, módulos y configuración común.</p><code>/api/hub/config</code></div></div></main></body></html>`);});
module.exports=router;
